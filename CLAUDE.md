# CLAUDE.md — project brief

## What this is
This is a **placeholder repository** reserved for the **LLM project**. There is
no real code here yet — every file is an intentional placeholder so the repo has
a starting structure.

## How to help
- The operator is **not a programmer** — do the technical steps for them and
  explain plainly. Only ask them for things only they can provide (e.g. API
  keys, decisions about scope).
- This repo is **public**, so never add secrets, API keys, or personal/private
  writing here.
- When real work starts, replace the placeholder files (`src/placeholder.py`,
  `docs/placeholder.md`) with actual content and update this brief + the README.

## Layout
- `src/`   — source code (placeholder for now)
- `docs/`  — documentation (placeholder for now)
- `.gitignore` — sensible Python/OS defaults
